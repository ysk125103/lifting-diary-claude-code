Commit any changes in the current branch and provide a suitable commit message based on the code changes. Then push the changes to remote. Next merge the current branch into the $1$ branch and resolve any issues off the back of that merge. Push the $1 branch to remote with `git push origin $1`. And stay in the current branch. If the argument is not provided, fail and show approprate message.
